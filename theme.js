import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4caf50', // Green shade for primary color
    },
    secondary: {
      main: '#81c784', // Lighter green for secondary color
    },
  },
});

export default theme;
